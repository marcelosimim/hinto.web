/**
 * Define o formato de retorno dos dados para o request feito pela tela de apresentação do sistema
 * @author rafaelvictor01
 */
export interface ILandingScreenResponseDTO {
  synopsis: string
  movieTitle: string
  urlImage: string
}
